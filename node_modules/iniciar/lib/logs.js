'use strict'
const chalk = require('chalk')
const log = console.log
const logError = str => log(chalk.red(' ✗ ') + str)
const logStep = str => log(chalk.green(' ✓ ') + str)

module.exports = {
  log,
  logError,
  logStep
}
