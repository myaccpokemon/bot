/**
 * Everything inside src/ is transpiled to public/
 * See package.json script "babel-watch"
 */

const reduceArrow = function (arr = [1, 2, 3]) {
  return arr.reduce((acc, val) => acc + val, 0)
}
