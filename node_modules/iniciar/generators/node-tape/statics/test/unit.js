const test = require('tape')
const add = require('../example.js')
test('add function output', function ({plan, equal}) {
  plan(1)
  const actual = add(1,3,5)
  const expected = 9
  equal(actual, expected, 'add() should return the sum of the arguments')
})
